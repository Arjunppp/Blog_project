<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
            <% if(locals.message){ %>
                 <h1>
                    <%= message %>
                 </h1>
              <% } %>

            <% if(locals.allBlogs) { %>
                <% allBlogs.forEach((each)=> { %>
                    <h1>
                        <%= each.title %>
                    </h1>
                    <h3>
                        <%= each.content %>
                    </h3>
                    <% }) %>

                        <% } %>
</body>

</html>